import colours from './ColourConfig';

const globalStyles = {};

globalStyles.topBarText = {
  color: colours.white,
  fontSize: 20,
};

globalStyles.squareText = globalStyles.topBarText;

module.exports = globalStyles;